Server = "https://gts.wat"

# need to get this from the http headers from a client like semaphore.social.
# GTS' oauth support doesn't deal with the oob URIs that folks like this code need
AccessToken = "wat"
